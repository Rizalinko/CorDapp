# Notary node: local dev network (consumed by the network bootstrapper, then
# used verbatim by the node at runtime). Mirrors what generate-config.sh emits.
myLegalName = "O=Notary, L=London, C=GB"
p2pAddress = "notary:10200"
detectPublicIp = false
devMode = true
crlCheckSoftFail = true
jarDirs = [ "/opt/corda/drivers" ]
cordappDirectories = [ "/var/lib/corda/cordapps", "/opt/corda/cordapps-baked" ]

rpcSettings {
    address = "0.0.0.0:10201"
    adminAddress = "0.0.0.0:10202"
}

rpcUsers = [
    { username = "corda", password = "dev-rpc-password", permissions = ["ALL"] }
]

dataSourceProperties {
    dataSourceClassName = "org.postgresql.ds.PGSimpleDataSource"
    dataSource.url = "jdbc:postgresql://postgres:5432/cordanotary"
    dataSource.user = "corda"
    dataSource.password = "devpassword"
}

notary {
    validating = false
}
