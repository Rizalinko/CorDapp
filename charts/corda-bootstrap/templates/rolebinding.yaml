apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "corda-bootstrap.serviceAccountName" . }}
  labels:
    {{- include "corda-bootstrap.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "corda-bootstrap.serviceAccountName" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "corda-bootstrap.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
